version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
  disable:
    - godot
    - exhaustruct

issues:
  exclude-rules:
    - path: "_test.go"
      linters:
        - errcheck
        - govet
